name: Ssh to Linux

on:
  workflow_dispatch

jobs:
  Build:
    runs-on: ubuntu-latest
    timeout-minutes: 61
    steps:

    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Setup tmate session
      uses: mxschmitt/action-tmate@v3

